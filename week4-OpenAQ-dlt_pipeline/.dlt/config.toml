# put your configuration values here

[runtime]
log_level="WARNING"  # the system log level of dlt
# use the dlthub_telemetry setting to enable/disable anonymous usage data reporting, see https://dlthub.com/docs/reference/telemetry
dlthub_telemetry = true
request_max_attempts = 10           # Retry 10 times (not 5)
request_timeout = 120               # Wait 120 seconds (not 60)
request_backoff_factor = 2          # Slower backoff
request_max_retry_delay = 60        # Cap delay to 60 seconds